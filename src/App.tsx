import { Craftfolio } from "./index";

/**
 * Local demo / playground. This is NOT part of the published package
 * (only `src/index.ts` and its dependencies are shipped).
 *
 * With no `data` prop it renders the built-in example content; pass a
 * `data` object to see your own content.
 */
export default function App() {
  return <Craftfolio />;
}
